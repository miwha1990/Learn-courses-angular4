import { Routes } from '@angular/router';

import { TermsPageComponent } from './terms-page.component';

export const TermsPageRoutes: Routes = [
    { path: 'terms-of-service', component: TermsPageComponent }
];
